version: 2.1
orbs:
  node: circleci/node@5.2.0

jobs:
  setup:
    docker:
      - image: circleci/node:21.7.3-browsers
    steps:
      - checkout
      - node/install-yarn
      - run: node --version
      - run: yarn --version
workflows:
  ci:
    jobs:
      - setup

  
